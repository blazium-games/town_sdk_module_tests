# Town Sdk Module Tests

A test suite and example project for the **Town Sdk** module built for the Blazium Engine.

## Features
- Fully integrated with the native C++ **Autowork** testing framework.
- Executes tests in headless mode.

## Running Tests
To run the test suite natively:

```bash
blazium --headless -s run_tests.gd
```
